# healthhub — fully pinned dependencies (reproducible installs).
# All versions are exact (==) so the integrity guarantees are tested against a known stack.

# ---- runtime ----
numpy==2.4.6
pandas==3.0.4
python-dateutil==2.9.0.post0
six==1.16.0                  # transitive: required by python-dateutil

# ---- test / dev ----
pytest==9.1.1
hypothesis==6.155.7
freezegun==1.5.5
